# Relative by default: dev requests go through Vite's /api proxy to the backend on
# :5000, which keeps CORS out of the picture and means testing from a phone on the
# LAN needs no per-device change.
#
# Set an absolute URL only for a cross-origin deployment.
VITE_API_BASE_URL=/api/v1
